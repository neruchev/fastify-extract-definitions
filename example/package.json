{
  "name": "example",
  "version": "3.0.1",
  "main": "./src",
  "license": "MIT",
  "scripts": {
    "postinstall": "yarn run build",
    "build": "tsc -b",
    "watch": "tsc-watch --onSuccess \"yarn run start\"",
    "start": "cross-env NODE_PATH=build/ node ./build"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "fastify": "^4.21.0",
    "fastify-extract-definitions": "^3.0.1",
    "tsc-watch": "^6.0.4",
    "typescript": "^5.1.6"
  }
}
