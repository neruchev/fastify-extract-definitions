{
  "name": "example",
  "version": "2.1.1",
  "main": "./src",
  "license": "MIT",
  "scripts": {
    "postinstall": "yarn run build",
    "build": "tsc -b",
    "watch": "tsc-watch --onSuccess \"yarn run start\"",
    "start": "NODE_PATH=build/ node ./build"
  },
  "dependencies": {
    "fastify": "^4.3.0",
    "fastify-extract-definitions": "^2.1.1",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.7.4"
  }
}
