{
  "name": "example",
  "version": "0.0.4",
  "main": "./src",
  "license": "MIT",
  "scripts": {
    "postinstall": "yarn run build",
    "build": "tsc -b",
    "watch": "tsc-watch --onSuccess \"yarn run start\"",
    "start": "NODE_PATH=build/ node ./build"
  },
  "dependencies": {
    "fastify": "^3.29.0",
    "fastify-extract-definitions": "^0.0.4",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.7.4"
  }
}
